# plumb-line remediation record

```
remediation-format: v1
source-report:       claude-code-autoresume/plumb-line-audit.md
source-report-format: v3
principles-revision: 1
date:                2026-07-23
commit:              3b64bd2 (before remediation; applied as 6e4900b)
```

Scope: the five findings the audit's disposition carried to this repo, applied
as design-doc requirements (this repo is docs-only; Phase 0/1 implement them).

| Finding | Path | Class | Action | Change summary |
| ------- | ---- | ----- | ------ | -------------- |
| Guessed sid laundered (P3 — Confidence + provenance) | `DESIGN.md` schema + wrapper | mechanical | applied-mechanical | `sid_source: injected\|guessed\|verified` in schema v1; re-verification promoted from "TBD" to requirement; 2026-07-21 incident cited |
| Hardcoded priors (P5 — Injectable priors) | `DESIGN.md` config | mechanical | applied-mechanical | Enumerated prior list becomes the config floor; new priors join at introduction time |
| Uncontracted outputs (P7 — Contracted outputs) | `DESIGN.md` web | mechanical | applied-mechanical | API payloads get version constant + canonical keys + importable validator |
| Unenforced layering (P2 — One-way layering) | `DESIGN.md` architecture, `ROADMAP.md` Phase 0 | mechanical | applied-mechanical | Ruleset file + CI import-linter required day one; composition root = CLI entry |
| Invisible config defaults (P3 — Confidence + provenance) | `DESIGN.md` config | mechanical | applied-mechanical | `crr config --effective` with per-key `configured\|default` origin |

Proposed (not applied): none.
Blocked / applied-conservative: none — all fixes were requirement-encoding;
no epistemic value had to be invented.

Verification: document-consistency checks only (5 audit-requirement markers,
3 `sid_source` references, clean 2-file diff); the repo has no runnable
enforcement yet — mandating that tooling (Phase 0) is itself this run's
P2 — One-way layering fix.

Companion fixes applied directly in the source repo (commit b5c3aa1 there):
archive lineage (P8 — State-first lineage), `zombie_strikes` config
(P5 — Injectable priors), linkstatus + PRODUCT.md maturity corrections
(P6 — Maturity vocabulary).

---

# Remediation run 2 — 2026-08-01 (baseline audit of the implemented repo)

```
remediation-format: v1
source-report:       plumb-line-audit.md
source-report-format: v3
principles-revision: 1
date:                2026-08-01
commit:              3afca50 (pre-remediation) → 11b350d (post, branch fix/plumb-line-remediation)
```

| Finding | Path | Class | Action | Change summary |
|---|---|---|---|---|
| F1 docstring v2 | `crr/core/status.py` | mechanical | applied-mechanical | docstring names v3 (`faabb1f`) |
| F2–F4 page priors | `page.html`/`config.py`/`web.py` | mechanical | applied-mechanical | `confirm_arm_seconds`, `notice_seconds`, `reload_delay_ms`, `diag_error_display_cap` injected via placeholders; PAGE_VERSION 14 (`e621b56`) |
| F5 doctor timeout | `crr/cli.py` | mechanical | applied-mechanical | reads `interop_timeout_seconds` (`e621b56`) |
| F6 port ×4 | `crr/cli.py`/`config.py` | mechanical | applied-mechanical | `dashboard_port` key; argparse `default=None` → config fallback (`e621b56`) |
| F7 RestartSec | `crr/adapters/systemd.py` | mechanical | applied-mechanical | `web_restart_seconds` threaded (`e621b56`) |
| F8 doctor versions | `crr/cli.py` | mechanical | applied-mechanical | all six contract versions printed (`faabb1f`) |
| F9 effective version | `crr/cli.py` | mechanical | applied-mechanical | `# defaults version: N` header (`faabb1f`) |
| F10 artifact stamps | systemd/shims/cli | mechanical | applied-mechanical (partial) | units + shims stamped `# generated by crr <ver> (config-defaults vN)` (`11b350d`); plists cannot carry comments (plistlib) and schtasks emits commands not files — both remain unstamped |
| F11 diagnostics params | `crr/core/diagnostics.py`/`contracts.py` | mechanical | applied-mechanical | required `params` dict (per-source caps/lookback/timeout); DIAGNOSTICS_CONTRACT_VERSION 2→3 (`f040170`) |
| F12 source/boots rendered | `crr/cli.py`/`page.html` | mechanical | applied-mechanical | `crr diagnose` prints source + boot; dashboard panel header shows source · boot (`f040170`) |
| F13 CLI dup honesty | `crr/cli.py` | mechanical | applied-mechanical | `[dup]` vs `[dup? guessed]`; non-injected sid_source shown (`faabb1f`) |
| F14 terminal-outcome pids | `crr/cli.py` | mechanical | applied-mechanical | revive prints gave-up pids; gc prints removed sid8s (`faabb1f`) |
| F15 archive read path | `crr/cli.py` | judgment | applied-judgment (builder: yes, CLI-only) | `crr archive --list`: reason · archived_at · sid8 · cwd (`8c6cd2a`) |
| F16 tmux tri-state | `crr/adapters/tmux.py`/`reviver.py`/`ops.py` | judgment | applied-judgment (builder: yes) | `list_sessions() → set \| None`; reviver skips the pass on None (no strikes on unknown); ops refuse honestly; rescue paths silent on unknown (`8c6cd2a`) |
| F17 sleep(0.1) | `crr/adapters/process_probe.py` | — | proposed | implementation-detail poll rate, bounded by config grace — not applied |
| F18 MODEL_TAIL_LINES | `crr/adapters/transcript_source.py`/`config.py` | judgment | applied-judgment (builder: yes) | `model_tail_lines: 200` key, param threaded, empirical rationale in config comment (`e621b56`) |
| F19 golden baseline | repo-wide | advisory | skipped (declared `planned`) | remains the tracked adoption gap |

Proposed (not applied): F17 poll-granularity naming; `/api/archive` + dashboard
archive panel (builder chose CLI-only); launchd-plist stamping (needs a
launchd-tolerated key, tolerance unverified) and schtasks (no file artifact).

Needs builder review: F10 deliberately partial (above); F16 limitation —
`crr rescued`/`rescue-check` render *unknown* tmux state identically to
*empty* (honest-but-coarse); ride-along fix `10df690` (doctor double-reported
a malformed config.toml — regression from `e621b56`, caught in self-review).

Verification: `627 passed, 31 skipped` · `lint-imports: 1 kept, 0 broken` ·
branch guard blocks code-on-main (exit 1) · every commit passed the
pre-commit gate (full suite + lint) at commit time.
